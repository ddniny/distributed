# Defines which nodes are used in the system and how to connect to them.
# This element should only be referenced during the constructor's initial
# setup.
# All names must be unique.
# No other nodes are known.
# These names will be used for src and dest addresses for the Message
# The local node's name is included in this list
Configuration :
  - Name : logger 
    IP   : localhost
    Port : 6666

  - Name : alice 
    IP   : localhost
    Port : 12345        # This is the incoming port on which MP will listen
                        # May be changed for further communication as usual
  - Name : bob 
    IP   : localhost
    Port : 14255
    
  - Name : charlie
    IP   : localhost
    Port : 12998
    
  - Name : daphnie
    IP   : 128.237.228.38
    Port : 1987

SendRules :
  - Action : drop # Ack message number 4 from bob to alice will not be sent
    Dest   : bob
    Kind   : Ack
  - Action : delay # Every Lookup message in the system will be delayed
    Kind   : Lookup
  - Action : duplicate      # 3rd message from alice that anyone 
    Src    : alice        # receives will be duplicated
    Kind   : Ack
  - Action : delay      # 3rd message from alice that anyone 
    Dest   : bob        # receives will be duplicated

ReceiveRules :
  - Action : duplicate      # 3rd message from Charlie that anyone 
    Kind   : dup
  - Action : delay
    Kind   : Ack

ClockType :
  - Type  : VectorClock

Groups :
  - Name : Group1
    Members :
        - alice
        - bob
        - charlie
  - Name : Group2
    Members:
        - daphnie
        - bob
        - charlie

